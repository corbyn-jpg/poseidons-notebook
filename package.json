{
  "name": "poseidons-notebook",
  "version": "1.0.0",
  "description": "Poseidon's Notebook - Marine Species Tracking",
  "scripts": {
    "heroku-postbuild": "cd frontend && npm install && npm run build",
    "start": "cd backend && npm start"
  },
  "engines": {
    "node": "18.x"
  }
}